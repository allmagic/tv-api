<!-- Content Header (Page header) -->
today: <%= currentDate %>
show: <%= testVariable %>
user_id from controller: <%= (typeof user_id != 'undefined') ? user_id : '' %>
user_id from session: <%= (typeof req.session.user_id != 'undefined') ? req.session.user_id : '' %>
<!-- /.content -->
